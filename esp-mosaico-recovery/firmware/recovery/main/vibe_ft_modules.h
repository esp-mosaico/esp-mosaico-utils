/* SPDX-License-Identifier: Apache-2.0
 * Recovery renders only GSPC's baked glyph packs. No runtime font file is
 * accepted, so FreeType's optional outline/bitmap font drivers are omitted.
 * This is FreeType's supported FT_CONFIG_MODULES_H customization hook. */
